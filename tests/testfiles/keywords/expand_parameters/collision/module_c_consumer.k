*KEYWORD
$# Module C does NOT define Rscale, just uses it
$# It should get the value from the parent deck (last one wins)
*SECTION_SOLID_TITLE
$# title
module C consumer
$#   secid    elform       aet
       300    &scale
$
*END
